---
description: Design a feature with RFC, scenarios, and test plan before implementation
---

1. Usa el tool `get_workflow` del MCP skylabs-mcp con `name: "sl-design-feature"` para obtener el workflow completo.
2. Seguí todos los pasos del workflow retornado al pie de la letra.
3. Los RFCs se guardan en `docs/rfcs/`. Usar el template en `docs/rfcs/_template.md`.
