---
description: Generate unit and integration tests for recent changes using Vitest
---

1. Usa el tool `get_workflow` del MCP skylabs-mcp con `name: "sl-generate-tests"` para obtener el workflow completo.
2. Seguí todos los pasos del workflow retornado al pie de la letra.
3. Este proyecto usa **Vitest** con jsdom para tests de hooks React. Los tests están en `src/test/`.
