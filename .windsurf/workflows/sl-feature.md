---
description: Implement a feature in the react-proto-kit library
---

1. Usa el tool `get_workflow` del MCP skylabs-mcp con `name: "sl-feature"` para obtener el workflow completo.
2. Seguí todos los pasos del workflow retornado al pie de la letra.
3. Tener en cuenta que este es un proyecto de **librería npm**, no un monorepo full-stack. No hay backend, frontend separado, ni deploy a infra. El output es un paquete npm publicado vía semantic-release.
